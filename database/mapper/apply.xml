<?xml version="1.0" encoding="UTF-8"?>
<sql id="apply">
	<!-- 申请 -->
	<insert id="addApply">
		insert into 
			apply(user_id,pid,state,auth,auth_detail,reason,remarks,gmt_apply)
		value
			(
				{{user_id | q}},
				{{pid | q}},
				{{state | q}},
				{{auth | q}},
				{{auth_detail | q}},
				{{reason | q}},
				{{remarks | q}},
				{{gmt_apply | dateFormat}}
			)
	</insert>
	<!-- 直接领导审批 -->
	<update id="leaderReview">
		update
			apply
		set
			pid_reason = {{pid | q}},
			state = {{state | q}},
			gmt_pid = {{gmt_now | dateFormat}}
	</update>
	<!-- 安全审批 -->
	<update id="safeReview">
		update
			apply
		set
			safe_reason = {{pid | q}},
			state = {{state | q}},
			gmt_safe = {{gmt_now | dateFormat}}
	</update>
	<!-- 终极审批 -->
	<update id="finalReview">
		update
			apply
		set
			final_reason = {{pid | q}},
			state = {{state | q}},
			gmt_end = {{gmt_now | dateFormat}}
	</update>
</sql>