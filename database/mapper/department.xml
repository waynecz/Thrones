<?xml version="1.0" encoding="UTF-8"?>
<sql id="department">
	<!-- 添加信息 -->
	<insert id="add">
		insert into
			department(name)
		value(
			{{name | q}}
		)
	</insert>

	<select id="all">
		select * from department
	</select>
	<insert id="add">
	    insert into
	        department(name)
	    values(
	        {{name | q}}
	    )
	</insert>
	<select id="get">
	    select
	        *
	    from
	        department
	    where
	        id = {{id | q}}
	</select>
</sql>