<?xml version="1.0" encoding="UTF-8"?>
<sql id="department">
	<select id="all">
		select * from department
	</select>
	<insert id="add">
	    insert into
	        department(name)
	    values(
	        {{name | q}}
	    )
	</insert>
</sql>